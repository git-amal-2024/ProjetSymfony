{% extends 'base.html.twig' %}

{% block title %}LigneCommande{% endblock %}

{% block body %}
    <h1>LigneCommande</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ ligne_commande.id }}</td>
            </tr>
            <tr>
                <th>Quantite</th>
                <td>{{ ligne_commande.quantite }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_l_c_index') }}">back to list</a>

    <a href="{{ path('app_l_c_edit', {'id': ligne_commande.id}) }}">edit</a>

    {{ include('lc/_delete_form.html.twig') }}
{% endblock %}
