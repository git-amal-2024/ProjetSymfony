<nav class="navbar navbar-expand-lg bg-body-tertiary" data-bs-theme="dark">
    <div class="container-fluid">



            <ul class="navbar-nav me-auto mb-2 mb-lg-0"> <!-- Remplacez navbar-nav-scroll par navbar-nav -->

                    <li class="nav-item">
                        <a class="nav-link active" aria-current="page" href="{{ path('homepage') }}">Home</a>
                    </li>


                {% if is_granted("ROLE_ADMIN") %}

                    <li class="nav-item ">
                            <a class="dropdown-item" href="{{ path('homepage') }}" aria-expanded="false">Aministrateur</a>
                            <a class="dropdown-item" href="{{ path('app_categorie_index') }}">Liste des Catégories</a>
                            <a class="dropdown-item" href="{{ path('app_categorie_new') }}">Ajouter une Catégorie</a>
                            <a class="dropdown-item" href="{{ path('app_user_index') }}">Liste des Utilisateurs</a>
                            <a class="dropdown-item" href="{{ path('app_article_index') }}">Liste des Articles</a>
                    </li>
                {% endif %}

                <form class="d-flex" role="search">
                    <input class="form-control me-2" type="search" placeholder="Search" aria-label="Search">
                    <button class="btn btn-outline-success" type="submit">Search</button>
                </form>
            </ul>
    </div>
</nav>



